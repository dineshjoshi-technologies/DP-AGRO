import type { Db } from "@paperclipai/db";
type FirstAdminTransaction = Pick<Db, "execute" | "select" | "insert" | "update">;
export type FirstAdminClaimResult<T = unknown> = {
    status: "claimed";
    userId: string;
    value: T | null;
} | {
    status: "already_claimed";
    existingUserId: string | null;
    value: null;
};
export declare function claimFirstInstanceAdmin<T = unknown>(db: Db, input: {
    userId: string;
    onClaim?: (tx: FirstAdminTransaction) => Promise<T>;
}): Promise<FirstAdminClaimResult<T>>;
export {};
//# sourceMappingURL=first-admin-claim.d.ts.map