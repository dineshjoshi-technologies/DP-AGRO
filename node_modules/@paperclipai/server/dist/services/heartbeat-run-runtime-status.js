import { redactSensitiveText } from "../redaction.js";
export const HEARTBEAT_RUN_RUNTIME_STATUS_TTL_MS = 90_000;
export const MAX_HEARTBEAT_RUN_RUNTIME_STATUS_MESSAGE_CHARS = 180;
const runtimeStatusesByRunId = new Map();
function cloneStatus(status) {
    return {
        ...status,
        updatedAt: new Date(status.updatedAt),
    };
}
export function sanitizeHeartbeatRunRuntimeStatusMessage(message) {
    const normalized = message.replace(/\s+/g, " ").trim();
    const redacted = redactSensitiveText(normalized);
    if (redacted.length <= MAX_HEARTBEAT_RUN_RUNTIME_STATUS_MESSAGE_CHARS)
        return redacted;
    return `${redacted.slice(0, MAX_HEARTBEAT_RUN_RUNTIME_STATUS_MESSAGE_CHARS - 3)}...`;
}
function isExpired(status, now, ttlMs) {
    return now.getTime() - status.updatedAt.getTime() > ttlMs;
}
export function setHeartbeatRunRuntimeStatus(input) {
    const message = sanitizeHeartbeatRunRuntimeStatusMessage(input.message);
    if (!message) {
        clearHeartbeatRunRuntimeStatus(input.runId);
        return null;
    }
    const status = {
        companyId: input.companyId,
        issueId: input.issueId,
        agentId: input.agentId,
        runId: input.runId,
        phase: input.phase,
        message,
        updatedAt: input.updatedAt ? new Date(input.updatedAt) : new Date(),
    };
    runtimeStatusesByRunId.set(status.runId, status);
    return cloneStatus(status);
}
export function getHeartbeatRunRuntimeStatus(runId, expected) {
    const status = runtimeStatusesByRunId.get(runId);
    if (!status)
        return null;
    const now = expected?.now ?? new Date();
    const ttlMs = expected?.ttlMs ?? HEARTBEAT_RUN_RUNTIME_STATUS_TTL_MS;
    if (isExpired(status, now, ttlMs)) {
        runtimeStatusesByRunId.delete(runId);
        return null;
    }
    if (expected?.companyId !== undefined && status.companyId !== expected.companyId)
        return null;
    if (expected?.issueId !== undefined && status.issueId !== expected.issueId)
        return null;
    if (expected?.agentId !== undefined && status.agentId !== expected.agentId)
        return null;
    return cloneStatus(status);
}
export function clearHeartbeatRunRuntimeStatus(runId) {
    return runtimeStatusesByRunId.delete(runId);
}
export function clearAllHeartbeatRunRuntimeStatuses() {
    runtimeStatusesByRunId.clear();
}
export function sweepExpiredHeartbeatRunRuntimeStatuses(now = new Date(), ttlMs = HEARTBEAT_RUN_RUNTIME_STATUS_TTL_MS) {
    let swept = 0;
    for (const [runId, status] of runtimeStatusesByRunId) {
        if (!isExpired(status, now, ttlMs))
            continue;
        runtimeStatusesByRunId.delete(runId);
        swept += 1;
    }
    return swept;
}
//# sourceMappingURL=heartbeat-run-runtime-status.js.map